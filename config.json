{
  "name": "Matomo",
  "slug": "matomo",
  "version": "1.0.38",
  "description": "Matomo analytics with embedded MariaDB",
  "boot": "auto",
  "webui": "[PROTO:ssl]://[HOST]:[PORT:9000]",
  "init": false,
  "arch": ["aarch64", "amd64"],
  "panel_icon": "mdi:chart-line",

  "ports": {
    "80/tcp": 9000
  },
  "ports_description": {
    "80/tcp": "Matomo web interface"
  },

  "map": [
    "ssl",
    "data"
  ],

  "options": {
    "ssl": true,
    "certfile": "fullchain.pem",
    "keyfile": "privkey.pem",
    "db_name": "matomo",
    "db_user": "matomo",
    "db_password": "matomo"
  },
  "schema": {
    "log_level": "list(trace|debug|info|notice|warning|error|fatal)?",
    "ssl": "bool",
    "certfile": "str",
    "keyfile": "str",
    "request_size_limit": "int?",
    "db_name": "str",
    "db_user": "str",
    "db_password": "str"
  }
}